/**
 * Feature flags — flip to enable experimental or expensive features.
 */

/** Dyadic combinatoriality analysis is very compute-intensive; disabled by default. */
export const ENABLE_DYADIC_ANALYSIS = false;
